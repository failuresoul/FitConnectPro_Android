<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/bg_gradient_primary"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="16dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/ivTrainerProfile"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:contentDescription="Nav Header Icon"
        android:paddingTop="8dp"
        android:src="@mipmap/ic_launcher_round" />
        <!-- Ideally we use a specific drawable like ic_admin_profile but trainer likely doesn't have one yet, using launcher round is ok or maybe ic_person if available. Sticking to launcher for now as placeholder -->

    <TextView
        android:id="@+id/tvTrainerNameHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:text="Trainer Name"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Trainer Dashboard"
        android:textColor="@android:color/white"
        android:textSize="14sp" />

</LinearLayout>
